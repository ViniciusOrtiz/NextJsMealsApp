export default function NotFoundError() {
    return (
        <main className="not-found">
            <h1>Not found</h1>
            <p>Unfortunately, we could not find page or resource</p>
        </main>
    )
}